# AI-Trainer Environment Variables

# Database (Shared with VendiOnline.EU)
DATABASE_URL=postgresql://username:password@hostname:port/database

# AI API Keys
OPENAI_API_KEY=your-openai-api-key-here
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# AI-Trainer Environment Variables
# Copia questo file in .env e compila con i valori corretti

# API Key per l'autenticazione (genera la tua con: node -e "console.log('ai-trainer-' + require('crypto').randomBytes(32).toString('hex'))")
AI_TRAINER_API_KEY=your-api-key-here

# Porta del server (default: 3000)
PORT=3000

# Ambiente di sviluppo
NODE_ENV=development

# Note:
# - Non condividere mai il file .env sui repository pubblici
# - Usa sempre variabili d'ambiente per le credenziali
# - La tua API key dovrebbe iniziare con 'ai-trainer-' seguita da 64 caratteri esadecimali

# VendiOnline.EU Integration
VENDIONLINE_API_URL=http://localhost:3000
# VENDIONLINE_API_URL=https://vendionline-eu.railway.app

# Railway Production Variables (set these in Railway dashboard)
# DATABASE_URL will be provided by Railway
# Set other variables in Railway project settings
